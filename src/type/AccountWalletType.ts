export interface Accountwallet {
    id: string;
    imgURL: string;
    name: string;
    balance: string;
    currency: string;
}

export interface Option {
    imgURL: string;
    name: string;
    currency: string;
    balance: number
  }
  