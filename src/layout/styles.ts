/** @format */

import styled from "styled-components";

export const Wrappper = styled.div`
  display: grid;
  grid-template-columns: 25% 75%;
  margin-top: 4em;
  padding: 0 10em;

  .children {
    padding-left: 3em;
  }
`;
